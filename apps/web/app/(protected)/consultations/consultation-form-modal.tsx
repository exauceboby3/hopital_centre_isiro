'use client';import{Activity,BedDouble,CheckCircle2,Clock3,FileSignature,FlaskConical,HeartPulse,Save}from'lucide-react';import type{Dispatch,FormEventHandler,SetStateAction}from'react';import{ConsultationStructuredPrescription,type ConsultationPrescription}from'@/components/consultation-structured-prescription';import{Modal}from'@/components/modal';import{SearchableSelect}from'@/components/searchable-select';import{StatusBadge}from'@/components/status-badge';import{patientName}from'@/lib/display';import{type BillableService,type ClinicalForm,type Consultation,type ConsultationDecision,type ConsultationExam,type DoctorAvailability,decisionOptions,formatDate,resultValues,saveLabel,workflowLabel,}from'./consultations.model';interface ConsultationFormModalProps{editing:Consultation;form:ClinicalForm;setForm:Dispatch<SetStateAction<ClinicalForm>>;onSubmit:FormEventHandler<HTMLFormElement>;onClose:()=>void;submitting:boolean;activeExams:ConsultationExam[];validatedExams:ConsultationExam[];guidance:{title:string;detail:string};examSearch:string;setExamSearch:Dispatch<SetStateAction<string>>;groupedLabServices:Record<string,BillableService[]>;selectedExamIds:string[];setSelectedExamIds:Dispatch<SetStateAction<string[]>>;imagingServiceId:string;setImagingServiceId:Dispatch<SetStateAction<string>>;imagingServices:BillableService[];hospitalizationServiceId:string;setHospitalizationServiceId:Dispatch<SetStateAction<string>>;hospitalizationServices:BillableService[];transferDoctorId:string;setTransferDoctorId:Dispatch<SetStateAction<string>>;doctors:DoctorAvailability[];transferReason:string;setTransferReason:Dispatch<SetStateAction<string>>;existingPrescription?:ConsultationPrescription;onPrescriptionCreated:(prescription:ConsultationPrescription)=>void;}export function ConsultationFormModal({editing,form,setForm,onSubmit,onClose,submitting,activeExams,validatedExams,guidance,examSearch,setExamSearch,groupedLabServices,selectedExamIds,setSelectedExamIds,imagingServiceId,setImagingServiceId,imagingServices,hospitalizationServiceId,setHospitalizationServiceId,hospitalizationServices,transferDoctorId,setTransferDoctorId,doctors,transferReason,setTransferReason,existingPrescription,onPrescriptionCreated,}:ConsultationFormModalProps){const save=onSubmit;const handlePrescriptionCreated=onPrescriptionCreated;return(<Modal wide title={`Consultation · ${patientName(editing.patient)}`}eyebrow={`${editing.patient.medicalRecordNumber} · ${patientName(editing.doctor)}`}onClose={onClose}><form onSubmit={save}className="clinical-consultation-form"><div className={`consultation-workflow-state stage-${(editing.appointment?.journeyStage??'IN_CONSULTATION').toLowerCase()}`}><div><strong>{workflowLabel(editing)}</strong><span>{editing.appointment?.journeyStage==='LABORATORY'?'Le patient est au laboratoire. Cette consultation n’occupe plus le médecin.':editing.appointment?.journeyStage==='RETURN_TO_DOCTOR'?'Tous les résultats sont validés. Interprétez-les puis choisissez la suite du parcours.':editing.appointment?.journeyStage==='HOSPITALIZATION'?'La demande a quitté la consultation et attend le traitement administratif puis l’attribution du lit.':'Le patient est actuellement pris en charge par le médecin.'}</span></div><StatusBadge status={editing.status}/></div>{editing.signature&&(<div className="alert warning"><FileSignature size={18}/>Dossier signé le {formatDate(editing.signature.signedAt)} par {editing.signature.doctorName}. Toute correction devient un amendement tracé et devra être resignée.</div>)}{editing.vitalSigns[0]&&(<div className="clinical-vitals-strip"><HeartPulse size={18}/><span>Temp. {editing.vitalSigns[0].temperatureC??'—'} °C</span><span>TA {editing.vitalSigns[0].systolic??'—'}/{editing.vitalSigns[0].diastolic??'—'}</span><span>FC {editing.vitalSigns[0].pulse??'—'}/min</span><span>FR {editing.vitalSigns[0].respiratoryRate??'—'}/min</span><span>SpO₂ {editing.vitalSigns[0].oxygenPercent??'—'}%</span><span>Glycémie {editing.vitalSigns[0].bloodGlucoseMgDl??'—'} mg/dL</span></div>)}{(activeExams.length>0||validatedExams.length>0)&&(<section className="clinical-form-section consultation-exam-section"><div className="section-title"><span><FlaskConical size={16}/></span><div><strong>Examens de laboratoire</strong><small>Demandes en cours et résultats biologiquement validés</small></div></div>{activeExams.length>0&&(<div className="pending-exam-list">{activeExams.map((exam)=>(<article key={exam.id}><Clock3 size={16}/><div><strong>{exam.type}</strong><span>Statut : {exam.status} · demandé le {formatDate(exam.requestedAt)}</span></div></article>))}</div>)}{validatedExams.length>0&&(<div className="consultation-lab-results"><div className="consultation-results-title"><CheckCircle2 size={19}/><div><strong>Résultats prêts pour interprétation</strong><span>Ces résultats appartiennent à cette consultation et restent visibles pendant la décision.</span></div></div>{validatedExams.map((exam)=>{const values=resultValues(exam);return(<article className="consultation-result-card"key={exam.id}><div className="consultation-result-card-heading"><strong>{exam.type}</strong><span>Validé {exam.validatedAt?formatDate(exam.validatedAt):''}</span></div>{values.length?(<div className="table-scroll"><table className="compact-table"><thead><tr><th>Rubrique</th><th>Résultat</th><th>Unité</th><th>Référence</th><th>Note</th></tr></thead><tbody>{values.map((value)=>(<tr key={value.key}><td>{value.label}</td><td><strong>{value.value}</strong></td><td>{value.unit||'—'}</td><td>{value.reference||'—'}</td><td>{value.note||'—'}</td></tr>))}</tbody></table></div>):(<p className="consultation-result-text">{exam.result||'Résultat validé.'}</p>)}{exam.resultData?.conclusion&&(<p className="consultation-result-conclusion"><strong>Conclusion du biologiste :</strong> {exam.resultData.conclusion}</p>)}</article>);})}</div>)}</section>)}<section className="clinical-form-section"><div className="section-title"><span>1</span><div><strong>Motif et anamnèse</strong><small>Informations médicales initiales</small></div></div><div className="form-grid"><label className="field full"><span>Plainte principale*</span><textarea required rows={2}value={form.chiefComplaint}onChange={(event)=>setForm({...form,chiefComplaint:event.target.value})}/></label><label className="field full"><span>Histoire de la maladie actuelle*</span><textarea required rows={4}value={form.presentIllnessHistory}onChange={(event)=>setForm({...form,presentIllnessHistory:event.target.value})}/></label><label className="field full"><span>Compléments d’anamnèse</span><textarea rows={3}value={form.anamnesisComplements}onChange={(event)=>setForm({...form,anamnesisComplements:event.target.value})}/></label><label className="field full"><span>Antécédents, interventions, histoire familiale et allergies</span><textarea rows={4}value={form.medicalHistory}onChange={(event)=>setForm({...form,medicalHistory:event.target.value})}/></label></div></section><section className="clinical-form-section"><div className="section-title"><span>2</span><div><strong>Examen et synthèse</strong><small>Constatations et interprétation du médecin</small></div></div><div className="form-grid"><label className="field full"><span>Examen physique*</span><textarea required rows={5}value={form.physicalExamination}onChange={(event)=>setForm({...form,physicalExamination:event.target.value})}/></label><label className="field full"><span>Interprétation des examens paracliniques</span><textarea rows={4}value={form.paraclinicalExams}onChange={(event)=>setForm({...form,paraclinicalExams:event.target.value})}placeholder="Interprétez ici les résultats du laboratoire ou de l’imagerie."/></label><label className="field full"><span>Diagnostic / hypothèses diagnostiques*</span><textarea required rows={3}value={form.diagnosis}onChange={(event)=>setForm({...form,diagnosis:event.target.value})}/></label><label className="field full"><span>Conduite thérapeutique*</span><textarea required rows={4}value={form.treatmentPlan}onChange={(event)=>setForm({...form,treatmentPlan:event.target.value})}/></label></div></section><section className="clinical-form-section consultation-decision-section"><div className="section-title"><span>3</span><div><strong>Décision clinique</strong><small>Une seule orientation claire pour la suite du patient</small></div></div><div className="consultation-workflow-context"><strong>{guidance.title}</strong><small>{guidance.detail}</small></div><label className="field full"><span>Action à exécuter*</span><select required value={form.decision}onChange={(event)=>setForm({...form,decision:event.target.value as ConsultationDecision})}>{decisionOptions.map((option)=><option key={option.value}value={option.value}>{option.label}</option>)}</select></label>{form.decision==='LABORATORY'&&(<div className="clinical-decision-panel"><div className="decision-flow-note"><FlaskConical size={18}/><span><strong>Après enregistrement :</strong> le patient passe au laboratoire, la consultation devient « En attente » et le médecin est libre. Le retour médical sera créé seulement lorsque tous les résultats seront validés.</span></div><label className="field full"><span>Rechercher un examen</span><input value={examSearch}onChange={(event)=>setExamSearch(event.target.value)}placeholder="NFS, glycémie, paludisme…"/></label><div className="exam-catalog-groups">{Object.entries(groupedLabServices).map(([category,services])=>(<fieldset key={category}><legend>{category}</legend>{services.map((service)=>(<label className="check-row"key={service.id}><input type="checkbox"checked={selectedExamIds.includes(service.id)}onChange={(event)=>setSelectedExamIds((current)=>event.target.checked?[...current,service.id]:current.filter((id)=>id!==service.id))}/><span>{service.name}</span></label>))}</fieldset>))}</div></div>)}{form.decision==='IMAGING'&&(<div className="clinical-decision-panel"><div className="decision-flow-note"><Clock3 size={18}/><span>Le patient quitte temporairement la consultation. Le médecin devient disponible pendant l’examen.</span></div><SearchableSelect className="full"required label="Examen d’imagerie"value={imagingServiceId}onChange={setImagingServiceId}options={imagingServices.map((service)=>({value:service.id,label:service.name,description:service.category}))}/></div>)}{form.decision==='HOSPITALIZATION'&&(<div className="clinical-decision-panel hospitalization-referral-flow"><div className="decision-flow-note"><BedDouble size={18}/><span>Le médecin transmet l’indication. L’admission physique n’est pas faite dans cette fenêtre.</span></div><ol><li><strong>Médecin :</strong> enregistre l’indication et transmet la demande.</li><li><strong>Réception :</strong> vérifie le dossier administratif, sans paiement préalable.</li><li><strong>Équipe infirmière / hospitalisation :</strong> choisit la chambre, le lit et confirme l’admission.</li></ol><SearchableSelect className="full"required label="Type de séjour / tarif journalier"value={hospitalizationServiceId}onChange={setHospitalizationServiceId}options={hospitalizationServices.map((service)=>({value:service.id,label:service.name,description:service.category}))}/></div>)}{form.decision==='TRANSFER'&&(<div className="form-grid clinical-decision-panel"><SearchableSelect className="full"required label="Médecin destinataire"value={transferDoctorId}onChange={setTransferDoctorId}options={doctors.filter((doctor)=>doctor.id!==editing.doctor.id).map((doctor)=>({value:doctor.id,label:doctor.name,description:`${doctor.specialty} · ${doctor.availability}`}))}/><label className="field full"><span>Motif du transfert*</span><textarea required minLength={5}rows={3}value={transferReason}onChange={(event)=>setTransferReason(event.target.value)}/></label></div>)}{form.decision==='PRESCRIPTION'&&(<ConsultationStructuredPrescription consultationId={editing.id}patientId={editing.patient.id}diagnosis={form.diagnosis}existingPrescription={existingPrescription}onCreated={handlePrescriptionCreated}/>)}</section>{editing.signature&&(<label className="field full"><span>Raison de l’amendement*</span><textarea required minLength={5}rows={2}value={form.amendmentReason}onChange={(event)=>setForm({...form,amendmentReason:event.target.value})}/></label>)}<div className="modal-actions clinical-actions"><button type="button"className="secondary-button"onClick={onClose}>Fermer</button><button className="primary-button"disabled={submitting}>{submitting&&<Activity className="spin"size={16}/>}<Save size={17}/>{saveLabel(form.decision)}</button></div></form></Modal>);}